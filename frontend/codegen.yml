overwrite: true
schema: "http://localhost:8000/graphql"
generates:
  ./src/models.ts:
    config: {}
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
